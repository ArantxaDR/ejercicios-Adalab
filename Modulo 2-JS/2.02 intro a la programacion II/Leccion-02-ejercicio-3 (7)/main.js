'use strict';

const name = 'Araceli Lobo';

const titleElement = document.querySelector('.title');

titleElement.innerHTML = `El nombre de mi compañera es ${name}, y está compuesto por ${name.length} caracteres`;

console.log (`funciona`);